# 登録・更新

```sh
git clone [修正リポジトリ名].git

cd [修正リポジトリ名]
プログラム修正
package.jsonのバージョン変更

.git/configがkurosameになっていること
git commit ...
git push

git tag [バージョン] -> バージョンはnpmに合わす
git push --tags

npm publish --access=public
```
